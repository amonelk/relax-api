---
description: 
alwaysApply: true
---

# 项目核心规则

## 目标与边界

- 该项目用于从 GitHub 拉取工具代码并提供 Web API，优先满足可部署、可维护、够用即可。
- 部署环境资源受限（512M 内存），避免引入复杂架构或重型依赖。

## 实现原则

- 优先简单清晰的目录结构和代码逻辑，先可用再优化。
- 新增功能时复用现有模块，避免过度抽象与过早优化。
- 每次改动保持最小影响面，便于排查和回滚。

## 资源与性能约束

- 默认选择低内存方案：流式处理、按需加载、及时释放临时对象/文件。
- 控制并发与缓存体量，避免无上限队列、全量加载大文件。
- 依赖选择以轻量稳定为先，能不用就不用，能少用就少用。

## API 与错误处理

- API 返回结构保持稳定、字段命名一致，错误信息简洁可读。
- 对外错误提示避免泄露内部细节，对内日志保留定位所需上下文。
- 输入参数必须做基础校验，失败尽早返回。

## 文档与验证

- 改动后同步更新最小必要文档（Readme、接口说明或示例）。
- 能快速验证的改动，优先补充最小测试或可复现验证步骤。

## 沟通约定

- 思考与回复默认使用中文。
- 回答问题时优先从整个代码库视角给出结论，再落到具体文件与改动建议。
